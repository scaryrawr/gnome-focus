{
  "name": "focus",
  "version": "1.10.0",
  "description": "Transparent inactive windows",
  "type": "module",
  "private": true,
  "engines": {
    "node": "22.x",
    "pnpm": "11.x"
  },
  "scripts": {
    "build:meta": "node generate-metadata.js",
    "build:schemas": "node generate-schemas.js",
    "clean": "node clean.js",
    "lint:oxlint": "oxlint . --deny-warnings",
    "typecheck": "tsgo --noEmit",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "pnpm lint:oxlint && pnpm typecheck && pnpm format:check",
    "bundle": "rolldown -c",
    "build": "pnpm lint && pnpm bundle && pnpm build:meta && pnpm build:schemas",
    "build:package": "pnpm clean && pnpm build && cd ./dist && zip -r ../focus@scaryrawr.github.io.zip . && cd ..",
    "package:uninstall": "[ -d ~/.local/share/gnome-shell/extensions/focus@scaryrawr.github.io ] && rm -r ~/.local/share/gnome-shell/extensions/focus@scaryrawr.github.io || echo ''",
    "package:install": "pnpm build:package && pnpm package:uninstall && mkdir -p ~/.local/share/gnome-shell/extensions && unzip focus@scaryrawr.github.io.zip -d ~/.local/share/gnome-shell/extensions/focus@scaryrawr.github.io && glib-compile-schemas ~/.local/share/gnome-shell/extensions/focus@scaryrawr.github.io/schemas"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/scaryrawr/gnome-focus.git"
  },
  "keywords": [
    "gjs",
    "gnome",
    "shell",
    "extension",
    "focus"
  ],
  "author": "Mike Wallio",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/scaryrawr/gnome-focus/issues"
  },
  "homepage": "https://github.com/scaryrawr/gnome-focus#readme",
  "devDependencies": {
    "@girs/clutter-18": "^4.1.0",
    "@girs/gjs": "^4.1.0",
    "@girs/gnome-shell": "^50.0.4",
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "oxfmt": "^0.58.0",
    "oxlint": "^1.73.0",
    "rolldown": "^1.1.5"
  },
  "packageManager": "pnpm@11.11.0"
}
